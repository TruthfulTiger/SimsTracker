{
  "name": "SimsTracker",
  "version": "1.0.0",
  "dependencies": {
    "shards-ui": "^3.0.0",
    "yuicompressor": "^2.4.8"
  },
  "devDependencies": {
    "flow-bin": "^0.85.0"
  },
  "scripts": {
    "build": "babel src/ -d lib/",
    "prepublish": "npm run build",
    "flow": "flow"
  }
}
